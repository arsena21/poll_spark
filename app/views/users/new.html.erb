
<% if !current_page? root_path %>
	<% provide :title do  %>
		<% "Sign up" %>
	<%end%>
	<h1>Sign up</h1>
<% end %>


<% if current_page? root_path %>
	<h3>
	Change 315 is a platform for collective action 	
	The Internet has changed the world. 
	Never has it been so easy to communicate, collaborate and  be heard. In the interconnected world, the ability to change policies, 
	organizations and behavior are within reach. 

	What would you change?
	<b>3 Votes</b> per person
	<b>1 Campaign</b> is launched every 6 months, based on popular vote
	For each campaign, we ask that everyone share with <b>5 Friends</b> to maximum reach 

	Change315 is a non-profit with a goal of making the world a better place, through the use of social networks and an interconnected populous. 
	</h3>
<% end %>


<!-- Sign-up form --!>

<% if !current_page? root_path %>
	<h3>
		Only 1 email will be delivered every 6 months. That's it!
	</h3>


		<%= form_for(@user) do |f| %>
		  <%= render 'shared/error_messages', object: f.object %>

		  <%= f.label :name %>
		  <%= f.text_field :name %>

		  <%= f.label :email %>
		  <%= f.text_field :email %>

		  <%= f.label :country %>
		  <%= f.select :country, [['Canada','Canada'],['United States','United States'],['Mexico','Mexico'],['France','France'],['UK','UK']] %>
		  
		  <%= f.label :password %>
		  <%= f.password_field :password %>

		  <%= f.label :password_confirmation, "Confirmation" %>
		  <%= f.password_field :password_confirmation %>

		  <%= f.submit "Signup!", class: "btn btn-large btn-primary" %>
		<% end %>

<% end %>

<!-- Skip signup form from root_path, or just Signin --!>

<div class="votes">
	<% if current_page? root_path %>
		<%= link_to "Sign up!", signup_path, class: "btn btn-large btn-primary" %>
		<%= link_to "Skip", petitions_path %>
	<% end %>
	<p>
		Already a user? <%= link_to "Sign in!", signin_path %>
	</p>
</div>